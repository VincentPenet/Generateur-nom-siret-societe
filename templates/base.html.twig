<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8"/>

    <meta name="viewport" content="width=device-width, initial-scale=1"/>

    <title>{% block title %}Simulateur de Siret{% endblock %}</title>

    <meta name="robots" content="noindex, nofollow"/>

    {# Logo Goupe NAT dans l'onglet de la page #}
    <link rel="icon" href="{{ asset('images/favicon-48x48.ico') }}"/>

    {# CSS #}
    {% block stylesheets %}

        {{ encore_entry_link_tags('app') }}

        {{ encore_entry_link_tags('global') }}

        {{ encore_entry_link_tags('styleNAT') }}

    {% endblock %}

</head>

<body>

{% block body %}{% endblock %}

{# JS #}
{# <script>src="{{ asset('build/runtime.js') }}</script> #}

{% block javascripts %}

    {{ encore_entry_script_tags('app') }}

    {{ encore_entry_script_tags('copy') }}

{% endblock %}

</body>

</html>